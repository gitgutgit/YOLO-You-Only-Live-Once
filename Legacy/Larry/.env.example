# 환경 설정 (Environment Configuration)
# .env.example을 복사하여 .env 파일 생성: cp .env.example .env

# 환경 모드 (development: 로컬, production: GCP)
ENVIRONMENT=development

# GCP Cloud Storage 설정
GCS_BUCKET_NAME=distilled-vision-game-data
GCP_PROJECT_ID=vision-final-478501

# Flask 설정
SECRET_KEY=game-secret-key-change-this
DEBUG=True
PORT=5002

# 로컬 데이터 디렉토리 (fallback)
LOCAL_DATA_DIR=./data
